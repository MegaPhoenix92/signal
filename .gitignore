node_modules/
dist/
*.tsbuildinfo
.tsbuildinfo/
vite.config.js
vite.config.d.ts
.DS_Store

# Local runtime state (never committed)
data/signal-local.json
data/signal-token-vault.json
data/provider-token-vault.json
data/signal-scheduler.lock
data/signal-state-service.json
data/signal-state-service-backups/

# Real env files (only the .example template is committed)
.env
.env.local
.env.production

# Screenshots / local evidence stay off the remote
evidence/

# Blueprint specs are committed (PRODUCT, BOUNDARY, milestones, specs/).
# Optional machine-local notes only:
docs/_local/

# Agent config files
.clinerules-*
